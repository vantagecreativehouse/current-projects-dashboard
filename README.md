# Current Projects Dashboard

A static v0 dashboard for testing the Vantage/Hermes GitHub → Vercel build/deploy loop.

## What it proves

- Local app scaffolding works
- Production build works
- GitHub push works
- Vercel deployment works

## Next useful version

Wire project cards to live Google Tasks / Drive / Hermes routing state instead of static source data.
